<mbean>
  <description>
  </description>

  <children>
    <ref-list title="Hosts">
      <name-pattern>resin:type=Host,Server=${this.objectName.name},*</name-pattern>
    </ref-list>
  </children>

  <attribute ignore="true" name="ObjectName"/>
  <attribute ignore="true" name="PortObjectNames"/>
  <attribute ignore="true" name="ClusterObjectNames"/>

  <attribute name="State">
    <category>statistic</category>
    <description>
    </description>
  </attribute>

  <attribute name="StartTime">
    <category>statistic</category>
    <description>
    </description>
  </attribute>

  <attribute name="InvocationCacheHitCount">
    <category>statistic</category>
    <description>
    </description>
  </attribute>

  <attribute name="InvocationCacheMissCount">
    <category>statistic</category>
    <description>
     </description>
  </attribute>

  <!-- XXX: ProxyCache should have it's own mbean
  <remove-attribute name="ProxyCacheHitCount"/>
  -->

  <attribute name="ProxyCacheHitCount">
    <category>configuration</category>
    <description>
     </description>
  </attribute>

  <!-- XXX: ProxyCache should have it's own mbean 
  <remove-attribute name="ProxyCacheMissCount"/>
  -->
  <attribute name="ProxyCacheMissCount">
    <category>configuration</category>
    <description>
     </description>
  </attribute>

  <!-- XXX: ProxyCache should have it's own mbean -->
<!--
  <command name="clearCache">
     <description>
       Invalidate all of the items in the cache.
     </description>
  </command>
-->

  <!-- XXX: ProxyCache should have it's own mbean -->
<!--
  <command name="clearCacheByPattern">
     <description>
       Invalidate all of the items in the cache that match a host and url 
       regular expression.
     </description>

     <arguments>
       <argument name="HostRegexp" type="regexp">
         <default>.*</default>
         <description>
         </description>
       </argument>
       <argument name="UrlRegexp" type="regexp">
         <default>.*</default>
         <description>
         </description>
       </argument>
     </arguments>
  </command>
-->

</mbean>
